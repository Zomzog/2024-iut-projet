server:
  port: 8080

logging:
  level:
    org.springframework.web: DEBUG
    iut.nantes.project: DEBUG
    org.hibernate: DEBUG

spring:
  application:
    name: products

  profiles:
    active: dev

---

spring:
  config:
    activate:
      on-profile: default
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password

