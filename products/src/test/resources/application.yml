spring:
  application.name: products
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:file:./data/productsdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true